# Journal Fullstack App 📓

## Structure:
- `/backend` → Spring Boot backend
- `/frontend` → React frontend

## How to Run:

### Backend (Spring Boot):
Open `/backend` in IntelliJ → Run `JournalApplication.java`

### Frontend (React):
Open `/frontend` in VS Code → Run:
```bash
npm install
npm start
